apiVersion: v1
kind: Service
metadata:
  name: neotool-web
  namespace: production
  labels:
    app: neotool-web
    component: web
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: neotool-web
