apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # External Secrets Operator HelmRelease only
  # The CRD-dependent resources (SecretStore, ExternalSecret, ServiceAccount)
  # are managed by the external-secrets-config Kustomization which depends on this
  - helmrelease.yaml
