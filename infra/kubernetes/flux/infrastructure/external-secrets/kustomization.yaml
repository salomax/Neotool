apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # External Secrets Operator HelmRelease
  - helmrelease.yaml
  
  # External Secrets configuration (applied after operator is ready)
  # These are applied separately to ensure operator is running first
  - service-account.yaml
  - secret-store.yaml
  - postgres-external-secret.yaml
