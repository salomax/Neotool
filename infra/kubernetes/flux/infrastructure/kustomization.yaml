apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Namespace first
  - namespace.yaml

  # Helm repositories
  - sources/helmrepositories.yaml

  # Vault (secrets management)
  - vault/helmrelease.yaml

  # External Secrets Operator (syncs Vault → K8s)
  - external-secrets/helmrelease.yaml
  - external-secrets/secret-store.yaml
  - external-secrets/postgres-external-secret.yaml
