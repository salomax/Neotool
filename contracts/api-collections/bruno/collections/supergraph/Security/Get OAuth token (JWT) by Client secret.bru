meta {
  name: Get OAuth token (JWT) by Client secret
  type: http
  seq: 32
}

post {
  url: http://{{security_host}}/oauth/token
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "grant_type": "client_credentials",
    "client_id": "service-test-asset",
    "client_secret": "assets:asset:view,assets:asset:upload,assets:asset:delete",
    "audience": "assets"
  }
}

settings {
  encodeUrl: false
  timeout: 0
}
