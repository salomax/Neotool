package io.github.salomax.neotool.comms.template.domain

/**
 * Communication channel types supported by the template engine.
 */
enum class Channel {
    EMAIL,
    PUSH,
    WHATSAPP,
    IN_APP,
    CHAT,
}
