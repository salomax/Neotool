package io.github.salomax.neotool.common.graphql.pagination

/**
 * Order direction for sorting in paginated queries.
 * Common component used across all services for flexible sorting.
 */
enum class OrderDirection {
    ASC,
    DESC,
}
